import { StudentQualificationDialogData } from './student-qualification-dialog-data';

describe('StudentQualificationDialogData', () => {
  it('should create an instance', () => {
    expect(new StudentQualificationDialogData()).toBeTruthy();
  });
});
