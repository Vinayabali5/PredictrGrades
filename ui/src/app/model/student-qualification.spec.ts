import { StudentQualification } from './student-qualification';

describe('StudentQualification', () => {
  it('should create an instance', () => {
    expect(new StudentQualification()).toBeTruthy();
  });
});
