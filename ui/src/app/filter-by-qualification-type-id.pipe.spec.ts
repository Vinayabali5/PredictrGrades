import { FilterByQualificationTypeIdPipe } from './filter-by-qualification-type-id.pipe';

describe('FilterByQualificationTypeIdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByQualificationTypeIdPipe();
    expect(pipe).toBeTruthy();
  });
});
